{% extends 'base.html.twig' %}

{% block title %}
	{{ product.title }}
{% endblock %}

{% block body %}
	<div class="container my-5">
		<div class="row">
			<h1 class="text-center mb-5">{{product.title}}</h1>
			<div class="col-md-6">
				<img src="{{product.picture}}" alt="{{product.title}}" class="img-fluid rounded">
			</div>
			<div class="col-md-6">
				<p class="text-muted">{{product.price}}€</p>
				<p>{{product.description}}</p>
				<a href="#">Ajouter au Panier</a>
			</div>
		</div>
	</div>
{% endblock %}

